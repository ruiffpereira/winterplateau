/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@kubb/plugin-client/clients/axios";
import type { RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { GetContentQueryResponse, GetContentQueryParams, GetContentHeaderParams, GetContent401, GetContent403, GetContent500 } from "../types/GetContent.js";

function getGetContentUrl() {
  const res = { method: 'GET', url: `http://localhost:3001/api/websites/content` as const }  
  return res
}

/**
 * @description Devolve um mapa chave→valor com o conteúdo traduzido do site.**Resolução do locale (por prioridade):**1. Customer autenticado (Bearer JWT no Authorization) → usa `Customer.defaultLanguage`2. Sem customer, `?locale=` enviado → usa o valor do parâmetro3. Sem customer, sem parâmetro → usa `User.defaultLanguage` do prestador**Identificação do prestador:**- Com customer JWT: enviar o site token em `x-site-token` e o customer JWT em `Authorization: Bearer`- Sem customer JWT: enviar o site token em `Authorization: Bearer` (comportamento original) ou em `x-site-token`
 * @summary Obter conteúdo CMS do prestador
 * {@link /websites/content}
 */
export async function getContent(params?: GetContentQueryParams, headers?: GetContentHeaderParams, config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetContentQueryResponse, ResponseErrorConfig<GetContent401 | GetContent403 | GetContent500>, unknown>({ method : "GET", url : getGetContentUrl().url.toString(), params, ... requestConfig, headers : { ...headers, ...requestConfig.headers } })  
  return res.data
}